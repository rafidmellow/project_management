/**
 * Re-export the useTaskContext hook from the task-context component
 * This allows for easier imports in components that need the task context
 */

import { useTaskContext } from '@/components/project/task-context';

export { useTaskContext };
